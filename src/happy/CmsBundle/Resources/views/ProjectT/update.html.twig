{% extends 'happyCmsBundle::base.html.twig' %}
{% block pageheader %}Төслийн төрөл засах хэсэг{% endblock %}

{% block body %}
    {% include ':inc:flashmsg.html.twig' %}
    {{ form_start(edit_form) }}
    {% if img %}
        <div class="form-group">
            <img src="/{{ statfolder }}{{ img }}" width="100">
        </div>
    {% endif %}
    {{ form_widget(edit_form) }}
    <input class="btn btn-success" type="submit" value="Засах"/>
    {{ form_end(edit_form) }}
    <a style="float: left;margin-bottom: 10px" href="{{ path('cms_project_type_show', {'id' : id, 'currentRoute':currentRoute}) }}">Буцах</a>
{% endblock %}
